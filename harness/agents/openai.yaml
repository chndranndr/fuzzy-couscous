interface:
  display_name: "Harness"
  short_description: "Build an adaptive agent-ready repo harness"
  default_prompt: "Use $harness init standard to make this repository agent-ready."

policy:
  allow_implicit_invocation: true


interface:
  display_name: "Kuskus"
  short_description: "Build an adaptive agent-ready coding workflow"
  default_prompt: "Use $kuskus init auto only to initialize or repair a repository harness; after initialization, follow the repository-local AGENTS.md operating contract for ordinary work and report verification evidence."

policy:
  allow_implicit_invocation: true

